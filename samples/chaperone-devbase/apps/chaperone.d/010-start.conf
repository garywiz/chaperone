# General environmental settings

settings: {
  env_set: {
  'PATH': '$(APPS_PATH)/bin:/usr/local/bin:/bin:/usr/bin:/sbin:/usr/local/sbin',
  'APPS_PATH': '$(_CHAP_CONFIG_DIR:-/)',
  #'SECURE_ROOT': '1',
  },
}

init.service: {
  type: oneshot,
  command: '/bin/bash $(APPS_PATH)/etc/init.sh',
  before: 'default,database,application',
  service_group: 'init',
}

chaperone.logging: {
  enabled: true,
  filter: '[chaperone].*',
  file: '$(APPS_PATH)/log/chaperone-%d.log',
}

syslog.logging: {
  enabled: true,
  filter: '*.info;![chaperone].*',
  file: '$(APPS_PATH)/log/syslog-%d.log',
}

console.logging: {
  enabled: true,
  stdout: true,
  filter: '*.warn;authpriv,auth.!*;daemon.!warn',
}
